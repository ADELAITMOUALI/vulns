# vulns

Static Intelligence Hub — simplified and published as `vulns` (Vulnerability Search).

This repository contains a static-first CVE viewer built with React + Vite and a small Express server for serving static data.

Setup (local):

```bash
# install
npm install

# development server
npm run dev
```

To publish to GitHub Pages or host elsewhere, see the repository settings.
